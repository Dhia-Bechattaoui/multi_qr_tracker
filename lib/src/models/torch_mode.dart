/// Enum representing the torch (flashlight) mode options for the camera.
enum TorchMode {
  /// Torch is always off.
  off,

  /// Torch automatically turns on in low light conditions.
  auto,

  /// Torch is manually controlled by the user via a button.
  manual,
}
